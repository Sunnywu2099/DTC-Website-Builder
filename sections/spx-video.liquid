

<style>
  .content-bg {
    background: #000000;
  }
  .content-bg .custom-container{
    margin-top: 0;
    margin-bottom: 0;
    padding-top: 96px;
  }
  .content-bg .title-box{
    width: 70%;
    margin: 0 auto;
  }
  .content-bg .title-box .title{
    font-size: 38px;
    line-height: 46px;
    color: #fff;
    text-align: center;
    margin-bottom: 16px;
  }
  .content-bg .title-box .text{
    font-size: 20px;
    line-height: 28px;
    color: #fff;
    text-align: center;
  }
  .content-bg .img-box{
    border-radius: 16px;
    overflow: hidden;
    margin-top: 48px;
    position: relative;
  }
  .content-bg .img-box .cover{
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .content-bg .post-player{
    width: 100%;
    object-fit: contain;
  }
  @media only screen and (max-width: 768px){
    .content-bg .custom-container{
      padding-top: 40px;
    }
    .content-bg .title-box{
      width: calc(100% - 32px);
    }
    .content-bg .title-box .title{
      font-size: 24px;
      line-height: 32px;
      margin-bottom: 8px;
      text-align: left;
    }
    .content-bg .title-box .text{
      font-size: 16px;
      line-height: 24px;
      text-align: left;
    }
    .content-bg .img-box{
      border-radius: 8px;
      margin-top: 24px;
    }
  }
</style>

<div class="content-bg">
  <div class="custom-container">
    <div class="title-box">
      {% if section.settings.title != blank %}
        <h2 class="title">{{ section.settings.title }}</h2>
      {% endif %}
      {% if section.settings.text != blank %}
        <p class="text">{{ section.settings.text }}</p>
      {% endif %}
    </div>
    <div class="img-box">
      {% if section.settings.video != blank %}
        <img class="cover" src="{{ section.settings.img | img_url:'master' }}">
        <video id="post-player2" class="post-player" playsinline muted autoplay loop>
          <source src="{{ section.settings.video }}" type="video/mp4" />
          <source src="{{ section.settings.video }}" type="video/webm" />
        </video>
      {% else %}
        <img class="hide-mb" src="{{ section.settings.img | img_url:'master' }}">
        <img class="hide-pc" src="{{ section.settings.img_m | img_url:'master' }}">
      {% endif %}
    </div>
    
  </div>
</div>

<script>
  $(document).ready(function() {
      let post_player = $('#post-player2'); 
      post_player.on('play', function() {
        $('.content-bg .cover').css('display','none');
      });
  });
</script>

{% schema %}
  {
    "name": "Content with image",
    "settings": [
          {
            "type": "textarea",
            "id": "video",
            "label": "Video link"
          },
          {
            "type": "image_picker",
            "id": "img",
            "label": "Image pc"
          },
          {
            "type": "image_picker",
            "id": "img_m",
            "label": "Image m"
          },
          {
            "type": "textarea",
            "id": "title",
            "label": "Title"
          },
          {
            "type": "textarea",
            "id": "text",
            "label": "Text"
          }
    ],
  "presets": [
    {
      "name": "Content with image"
    }
  ]
    
  }
{% endschema %}